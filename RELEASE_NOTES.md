## v0.3.6 更新内容

- 修复宽屏分页接管章节切换后，电脑端阅读进度未同步至番茄账号的问题。
- 正文实际进入并挂载成功后，调用番茄官方网页进度接口更新当前章节，手机端可继续读取该进度。
- 下一章预加载不会提前上报为已读；同一章节的重复渲染也不会重复提交。
- 保留“拦截网页统计上报”隐私选项，并明确该选项不影响必要的阅读进度同步。
- 新增进度字段、请求格式、章节去重和失败重试回归测试。
